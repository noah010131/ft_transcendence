import { OnModuleDestroy } from '@nestjs/common';
import { ConfigService } from '@nestjs/config';
import Redis from 'ioredis';
import { PresenceState } from './presence.types';
type PresenceFlags = {
    matching: boolean;
    inGame: boolean;
};
export declare class PresenceRedis implements OnModuleDestroy {
    private readonly configService;
    private readonly host;
    private readonly port;
    private readonly pub;
    private readonly sub;
    private readonly kv;
    constructor(configService: ConfigService);
    getPublisher(): Redis;
    getSubscriber(): Redis;
    createSubscriber(): Redis;
    addSocket(userId: string, socketId: string): Promise<number>;
    removeSocket(userId: string, socketId: string): Promise<number>;
    getSocketCount(userId: string): Promise<number>;
    getSocketIds(userId: string): Promise<string[]>;
    touchSocketAlive(userId: string, socketId: string, ttlSec: number): Promise<void>;
    clearSocketAlive(userId: string, socketId: string): Promise<void>;
    isSocketAlive(userId: string, socketId: string): Promise<boolean>;
    getEffectiveState(userId: string): Promise<PresenceState>;
    setEffectiveState(userId: string, state: PresenceState): Promise<void>;
    getFlags(userId: string): Promise<PresenceFlags>;
    setFlags(userId: string, flags: PresenceFlags): Promise<void>;
    getLastSequence(source: string, userId: string): Promise<number>;
    setLastSequence(source: string, userId: string, seq: number): Promise<void>;
    getLastEventAt(source: string, userId: string): Promise<number>;
    setLastEventAt(source: string, userId: string, at: string): Promise<void>;
    markEventProcessed(eventId: string, ttlSec?: number): Promise<boolean>;
    getUsersWithSockets(): Promise<string[]>;
    getFriendIdsCache(userId: string): Promise<string[] | null>;
    setFriendIdsCache(userId: string, friendIds: string[], ttlSec?: number): Promise<void>;
    invalidateFriendIdsCache(userIds: string[]): Promise<void>;
    onModuleDestroy(): Promise<void>;
    private socketsKey;
    private socketAliveKey;
    private effectiveKey;
    private flagsKey;
    private lastSeqKey;
    private lastEventAtKey;
    private eventDedupKey;
    private lastSeenKey;
    private friendIdsKey;
}
export {};
